# H3 Python SDK — Task Board

**Core purpose:** Python SDK for the H3 protocol — Pydantic models, BaseHarness ABC, FastAPI router, test bed (MockHermes), pytest suite (106 tests). Package: `h3-harness-sdk` (src-layout, hatchling).

Board lives in `.coding-hermes/board/` (JSONL-canonical: events.jsonl / tasks.jsonl / fixtures.jsonl / schema.sql tracked; board.db + *.parquet local caches — JSONL-NORM-001). This file mirrors active matrix rows for humans and guard validation.

## Active Tasks

| ID | Task | Pri | Cpx | Deps | Tags | Model | Reasoning | Fallback |
|----|------|-----|-----|------|------|-------|-----------|----------|
||| ✅ BUG-001 | 🐛 P1 — GET /v1/sessions/{id} 500s when get_session_info returns epoch-float started_at/last_active (canonical EchoHarness stored `time.time()` floats): SessionResponse.started_at is a protocol str → pydantic ValidationError → 500 INTERNAL_ERROR. Battery 43/43 is blind to real-session GET (only nonexistent→404) — caught by the E2E-001 live protocol loop (tick #83). ✅ COMPLETE tick #83 — router `_iso_timestamp()` coerces numeric epochs → ISO-8601 UTC strings (empty-string default preserved); echo.py stores `datetime.now(timezone.utc).isoformat()`; +1 regression test (float-epoch harness → 200 + ISO-parseable started_at). 124/124 tests, ruff 0, live 200 + ISO started_at on :8777, battery 43/43 post-fix. | P1 | 1 | — | +python, +testing | deepseek-v4-flash | Coerce epoch floats to ISO strings in router; fix echo.py example | deepseek-v4-flash |
||| ✅ DF-001 | 🐛 P0 — Wheel build excludes `__init__.py`: `.gitignore` pattern `_*.py` (meant for root temp scripts) also matches `h3_harness/__init__.py` + `examples/__init__.py`; hatchling applies gitignore patterns to wheel file selection → every non-editable install (`pip install <path|git|wheel>`) yields an empty namespace package → `from h3_harness import BaseHarness` → ImportError. Reproduced with `pip wheel` (no cache) and direct `hatchling build`. Fix: anchor the pattern to `/_*.py` (verified: wheel then contains both `__init__.py` files) and add a CI check that the built wheel contains `h3_harness/__init__.py`. | P0 | 1 | — | ++packaging, +python | deepseek-v4-flash | Anchor `_*.py` → `/_*.py`, rebuild wheel, verify contents, add CI wheel-content check | deepseek-v4-flash |
||| ✅ DF-002 | 🐛 P0 — `pip install h3-harness-sdk` fails: package is not published to PyPI ("No matching distribution found"). README + AGENTS.md both document this as THE install path. Fix: publish 0.1.0 to PyPI (after DF-001) or document `pip install git+https://github.com/get-h3/sdk-python` / editable source install until published. | P0 | 2 | DF-001 | ++packaging, +docs | deepseek-v4-flash | Publish to PyPI; add install-fallback section to README | deepseek-v4-flash |
|||| ✅ DF-003 | 🐛 P1 — Shipped `examples/langchain_agent.py` is broken at runtime: `LLMCall.messages` is `list[dict]` but the example passes `LLMMessage` objects → pydantic ValidationError on first /v1/process → router silently returns `decision=end, reason=error` (HTTP 200). Second latent bug: `req.result.type`/`req.result.data` on `ResultRequest.result` which is a plain `dict`. Fix: build `messages` as dicts; read result via `.get("type")`. | P1 | 1 | — | ++python, +docs | deepseek-v4-flash | Fix messages to dicts, fix result access, re-verify example with h3-test | deepseek-v4-flash |
|||| ✅ DF-004 | 🟡 P2 — Battery (h3-test) conventions undocumented: to score 43/43 a harness must (1) echo `context.history` in every Decision, (2) not issue LLM_CALL when `context.models` is empty, (3) return `text.finished=false` for "do not finish" prompts. README quickstart teaches none of these — a doc-following user scores 41/43 and must read battery source to find them (dogfood evidence: 41→42→43 journey). Fix: document in README/AGENTS or ship a "battery-ready" template harness based on the echo example. ✅ COMPLETE — README "Passing the battery" section + AGENTS.md Development pointer (3 conventions verified against shim test_battery.py: 2_8 history echo, 5_8 no llm_call on empty models, 2_4 finished=false), echo.py cited as 43/43 template, judge PASS 7/7 (5ae294a). | P2 | 2 | — | ++docs, +testing | deepseek-v4-flash | Add "Passing the battery" section to README with the 3 conventions | deepseek-v4-flash |
|||| ✅ DF-005 | P2 — `GET /v1/sessions/{id}` returns `started_at`/`last_active` always `""` even when the harness implements `get_session_info` and provides them (router only passes through `turn_count`). Cosmetic but visible in the protocol surface. Fix: pass through started_at/last_active when `get_session_info` returns them. ✅ COMPLETE — router now passes through started_at/last_active from get_session_info (`info.get("started_at","")`/`info.get("last_active","")`, empty-string defaults keep protocol str fields valid); +3 regression tests (SessionTrackingHarness: timestamp pass-through, omitted-key defaults, unknown-session 404); 113/113 tests, ruff 0, guard PASS, judge PASS 6/6 (1c63d4d). | P2 | 1 | — | +python | deepseek-v4-flash | Pass through session timestamps in router's SessionResponse | deepseek-v4-flash |
||||| ✅ NEVER-DONE | 11-point audit sweep → RETIRED: converted to dated bounded checklist docs/audit-2026-08.md (GAP-003) — 11 points answered pass/fail w/ evidence, tick #67 | High | 2 | — | ++code-review, +testing | deepseek-v4-flash | ✅ COMPLETE tick #67 — fixture no longer runs every tick; audit artifact committed | GLM-5.2 |
|||||| E2E-001 | E2E Testing Tick — perpetual fixture (self-improving loop; load coding-hermes-testing for F2B/B2F prompts): full E2E sweep each 5-tick window vs canonical EchoHarness :8777 (44/44 h3-test battery gate — battery grew 43→44 with test_5_9b, tick #94); window #98-103 open, closing tick #103 non-deferrable | High | 2 | — | ++testing | deepseek-v4-flash | Run E2E F2B/B2F sweep per window; 44/44 battery gate | deepseek-v4-flash |
||||| ✅ JSONL-NORM-001 | 🟡 P1 — Board storage: JSONL canonical (git-friendly) — untrack board.db/parquet (Bane directive 08-07). ✅ COMPLETE tick #83 — migrated: git rm --cached events.parquet/tasks.parquet, .gitignore *.parquet, exported tasks.jsonl/events.jsonl/fixtures.jsonl from board.db (NDJSON), parity MATCH (58/58). Tracked set: tasks.jsonl + events.jsonl + fixtures.jsonl + schema.sql. | P1 | 1 | — | +hygiene, +board | deepseek-v4-flash | JSONL canonical store — untrack board.db/parquet (Bane directive 08-07) | deepseek-v4-flash |
|||| GAP-001 | 🟡 P1 — Battery compliance has NO CI guard: CI (.github/workflows/ci.yml) runs lint/build/wheel-__init__-check/test only (`make test`); the 43/43 h3-test claim (README "Passing the battery", echo.py as 43/43 template — DF-004) is verified only by hand. ✅ COMPLETE tick #68 — new `battery` CI job (0304967): installs hermes-h3-shim, serves `scripts/serve_echo.py` (canonical EchoHarness wrapper on :9191) with uvicorn, health-waits, runs `h3-test --endpoint http://localhost:9191 | tee battery.log` with pipefail + `grep -q "43/43"` — exit gates the job. Local verification: 43/43 PASSED exit 0, 113/113 tests, ruff 0 + format clean, YAML parses, src/h3_harness/ untouched. Guard PASS, judge PASS 10/10. | P1 | 3 | — | ++testing, ++ci | deepseek-v4-flash | Add h3-test battery job to CI against echo.py; assert 43/43 | deepseek-v4-flash |
|||| ✅ GAP-002 | 🟡 P1 — README Quickstart teaches a battery-NON-compliant harness FIRST: the snippet returns `finished=True` with no history echo and no empty-models guard — a user who copies ONLY the quickstart scores <43/43 and must discover the separate "Passing the battery" section (or read battery source). DF-004 documented the conventions but left the quickstart itself as the trap. Fix: make the quickstart snippet battery-compliant (history echo, finished=false for do-not-finish, no llm_call on empty models) OR add an inline callout linking the battery section; add a test asserting the quickstart harness passes h3-test. Pass criteria: quickstart code run through h3-test = 43/43 (or explicit callout + CI test). | P1 | 2 | — | ++docs, +testing | deepseek-v4-flash | Make quickstart snippet battery-compliant or add callout + test | deepseek-v4-flash |
|||| ✅ GAP-003 | 🟢 P2 — NEVER-DONE 11-point audit sweep never completes: ticks #64-66 are "idle audit" commits while the audit task stays pending forever — the audit exists precisely to catch what the 08-03 dogfood found (DF-001..005). Fix: convert to a dated, bounded checklist — docs/audit-2026-08.md with the 11 points answered pass/fail + evidence — complete it, mark the task complete. ✅ COMPLETE tick #67 — docs/audit-2026-08.md committed (11/11 pass, docs+CI partial w/ tracked gaps), NEVER-DONE flipped to complete, event 36, commit <pending>. | P2 | 2 | — | ++code-review, +testing | deepseek-v4-flash | Replace NEVER-DONE fixture with dated 11-point audit checklist, complete it | GLM-5.2 |
|||| ✅ GAP-004 | 🟢 P2 — No API reference docs: docs/ has only dogfood reports; integrators must read source to learn BaseHarness methods, Decision/TextResponse fields, router options, MockHermes testbed API (README covers only the happy path + 1 testbed example). Fix: add a pdoc/mkdocs-generated (or hand-written) API reference for h3_harness committed under docs/api/, linked from README. Pass criteria: docs/api/ committed, README links it. | P2 | 2 | — | ++docs | deepseek-v4-flash | Add API reference docs for h3_harness, link from README | deepseek-v4-flash |
||| ✅ GAP-005 | 🔴 P0 — README line 88 `pip install hermes-h3-shim` fails: hermes-h3-shim is never published to PyPI ("No matching distribution found", re-verified 2026-08-04 stand-in PM sweep) — every user following the documented local battery-validation path ("Passing the battery" section) hits a 404; CI works around it by git-checkout of the shim repo (ci-gap-01). Fix: publish hermes-h3-shim to PyPI OR document `pip install git+https://github.com/get-h3/shim` as the fallback in README. Pass criteria: `pip install hermes-h3-shim` succeeds (or README documents a working from-source fallback). ✅ COMPLETE tick #73 — README "Passing the battery" now documents the from-source fallback (shim unpublished on PyPI; umbrella P3-10 still needs PYPI_API_TOKEN from Bane); fresh-venv install verified (exit 0, h3-test present) + full battery 43/43 via fresh-venv h3-test, judge PASS 7/7 (c6fd4f3). | P0 | 2 | — | ++packaging, +docs | deepseek-v4-flash | Publish shim to PyPI or document working fallback in README | deepseek-v4-flash |
|||| ✅ GAP-006 | 🟡 P1 — AGENTS.md quickstart is battery-NON-compliant: it returns `TextResponse(content="Hello from Python!", finished=True)` with no history echo and no empty-models guard — a developer who reads AGENTS.md FIRST (the agent-facing entry doc) builds a harness scoring 41/43, while the README quickstart is compliant (GAP-002 fixed README only, AGENTS.md left behind). Verified 2026-08-04 stand-in PM sweep: AGENTS.md snippet fails battery tests 2_4 (finished=false) and 2_8 (history echo). Fix: sync AGENTS.md quickstart with README's battery-compliant version (history echo + finished=false + no llm_call on empty models). Pass criteria: AGENTS.md quickstart code passes the same battery-convention assertions as README quickstart (test_quickstart.py or equivalent). ✅ COMPLETE tick #73 — commit a7f9203 (stand-in PM subagent, foreman-verified): AGENTS.md quickstart battery-compliant (history echo, finished=false heuristic, TEXT-only); test_quickstart.py extended to exec+assert the AGENTS.md snippet; 123/123 tests, ruff 0, guard PASS, judge PASS 7/7 (gap-006). | P1 | 2 | — | ++docs, +testing | deepseek-v4-flash | Sync AGENTS.md quickstart with README battery-compliant version | deepseek-v4-flash |
||||| ✅ GAP-007 | 🟢 P2 — tasks.md mirror lacks E2E-001 fixture matrix row (board.db fixtures has E2E-001 active; NEVER-DONE IS mirrored but E2E-001 is not) — fleet validator convention requires the fixture to appear in the mirror; currently only a prose mention exists. Found by PM 2026-08-05 (validator run + fixture diff). Fix: add an E2E-001 fixture matrix row to tasks.md mirror matching the NEVER-DONE row style. Pass criteria: `grep -c "^||||* E2E-001" .coding-hermes/tasks.md` >= 1 (actual matrix row with E2E-001 ID, not prose) AND validate-board-format.py PASSes. ✅ COMPLETE tick #75 — E2E-001 fixture matrix row added to mirror (NEVER-DONE row style, active fixture: window #73-78 open, closing tick #78 non-deferrable); anchored grep criterion met (1 matrix row) + validator PASS (5 tasks, 0 missing models), judge PASS 3/3 (gap-007). | P2 | 1 | — | +hygiene, +board | deepseek-v4-flash | Add E2E-001 fixture matrix row to tasks.md mirror so validator PASSes | deepseek-v4-flash |
|||||| ✅ GAP-008 | 🟢 P2 — Bare `python3 -m pytest` crashes: pyproject.toml addopts hardcodes pytest-benchmark flags (--benchmark-min-rounds=3 --benchmark-max-time=0.5); without pytest-benchmark installed, pytest exits 'unrecognized arguments' — the universal dev reflex `python3 -m pytest tests/ -q` on a fresh clone fails with what looks like a pytest version mismatch. Verified 2026-08-07 stand-in PM sweep (hunter probe). Fix: move benchmark tuning to a [tool.pytest-benchmark] section (parsed only when plugin present) or gate addopts; `make test` (venv, plugin installed) must keep working. Pass criteria: `python3 -m pytest tests/ -q` on a bare clone runs the non-benchmark tests cleanly or prints 'install pytest-benchmark for benchmarks' — never 'unrecognized arguments'. ✅ COMPLETE tick #82 — pytest-benchmark 5.2.3 registers NO ini keys (verified in plugin source), so the fix gates addopts instead: removed benchmark flags from pyproject addopts, moved tuning to module-level `pytestmark = pytest.mark.benchmark(min_rounds=3, max_time=0.5)` in tests/test_benchmarks.py (after importorskip). Verified: fresh venv WITHOUT pytest-benchmark → 115 passed + 1 skipped, zero 'unrecognized arguments'; venv full suite 123 passed incl. 8 benchmark tests. ruff 0, guard PASS, commit 3e5f710, judge PASS 4/4 (gap-008). | P2 | 1 | — | +python, +testing | deepseek-v4-flash | Move benchmark flags to [tool.pytest-benchmark] or gate addopts; bare pytest must not crash | deepseek-v4-flash |
|||||| ✅ GAP-009 | 🟢 P2 — `make build` produces NO artifact: target only verifies an import (prints 'build: OK', nothing in dist/); the real wheel build exists only as an inline CI step. A dev following the Makefile/README dev flow expects a wheel and gets nothing. Verified 2026-08-07 stand-in PM sweep (hunter probe). Fix: make `make build` actually build a wheel (`python -m build` or `pip wheel . -w dist/`), or rename the target to `make check` and update the README dev section. Pass criteria: `make build` creates dist/*.whl OR the target is renamed and README documents `make check`. ✅ COMPLETE tick #82 — Makefile `build` target now runs `uv build` (wheel + sdist into dist/); CI 'Verify wheel' step validates make build's own wheel (removed the duplicate `pip wheel` line that would break the exactly-one-wheel assertion); README dev section documents `make build # build wheel (and sdist) into dist/`. Verified: dist/h3_harness_sdk-0.1.0-py3-none-any.whl contains both required __init__.py files. ruff 0, guard PASS, commit c099e66, judge PASS 4/4 (gap-009). | P2 | 1 | — | +python, +build | deepseek-v4-flash | Make `make build` produce a wheel, or rename to `make check` + update README | deepseek-v4-flash |
|||||| ✅ GAP-010 | 🐛 P1 — AGENTS.md:8 `pip install h3-harness-sdk` dead install ref claim: "package never published to PyPI (E404)". Premise VERIFIED FALSE 2026-08-07 tick #86: h3-harness-sdk 0.1.0 IS on PyPI (uploaded 08-03T14:21Z; fresh-venv `pip install h3-harness-sdk` exit 0 + `from h3_harness import BaseHarness` import OK). Real gap: AGENTS.md lacked the from-source fallback that README has. ✅ COMPLETE tick #86 (foreman-direct, doc-only) — AGENTS.md Install section synced with README: kept `pip install h3-harness-sdk` + added "Install fallback (source / git)" (git+https + editable source). Pass criteria met: AGENTS.md documents from-source fallback (grep `git+https://github.com/get-h3/sdk-python` = line 18) AND fresh-venv AGENTS.md install yields importable h3_harness (verified, 0.1.0). 124/124 pytest, ruff 0, format 41 clean, guard PASS, commit 56e024c, co-author trailer verified. | P1 | 1 | — | +python, +build | deepseek-v4-flash | Sync AGENTS.md install section with README fallback; premise was stale (package IS published) | deepseek-v4-flash |
||||||| ✅ GAP-011 | 🟢 P2 — skills/h3-sdk-python-usage/SKILL.md dangerously stale: claims `pip install h3-harness-sdk` fails/not-on-PyPI (0.1.0 IS published, GAP-010), claims git/wheel installs yield broken wheel (fixed DF-001), claims langchain_agent.py crashes (fixed DF-003), references parquet board (now JSONL). Agents loading this skill get wrong install instructions and false crash warnings. Fix: sync SKILL.md with reality (PyPI 0.1.0 + from-source fallback; drop crash claim; JSONL board ref) + last-verified datestamp. PASS: grep 'not on PyPI' skills/h3-sdk-python-usage/SKILL.md = 0 AND grep -ic 'crash' = 0 AND 2026-08 datestamp present. ✅ COMPLETE tick #89 (foreman-direct, doc-only — imhotep S02 exception) — SKILL.md v1.0.1 synced: PyPI 0.1.0 primary install + git/editable from-source fallback, DF-001/DF-003 confirmed, JSONL board note, "Last verified: 2026-08-07" datestamp. Pass criteria verified: 0 / 0 / datestamp present. 124/124 pytest, ruff 0, format 41 clean, guard PASS, commit da6f8ed. | P2 | 1 | — | +docs, +skills | deepseek-v4-flash | Sync stale usage skill with current reality | deepseek-v4-flash |

## Stand-In Gap Findings (2026-08-04)

Hourly stand-in sweep (Bane away). Promise vs verified reality: **the core promise HOLDS** — fresh-venv `pip install h3-harness-sdk` (PyPI 0.1.0) succeeds, `from h3_harness import BaseHarness` works, installed wheel contains `__init__.py` + all modules (harness/middleware/protocol/testbed/examples); CI has a DF-001 wheel-content regression check. Remaining gaps: (1) no h3-test battery in CI (43/43 compliance unguarded); (2) README quickstart snippet itself is battery-non-compliant (first-code-seen trap, DF-004 left it); (3) NEVER-DONE audit sweep pending while foreman logs idle audits; (4) no API reference docs for integrators. All 4 written as GAP-001..004 above. Note: local `pytest` run exceeded 60s (time-boxed, killed) — suite duration worth checking in GAP-003's audit.
||||||| ✅ GAP-012 | 🐛 P1 — PyPI page README stale/non-compliant: pypi.org h3-harness-sdk 0.1.0 showed old quickstart ("Hello from Python!" without history echo, streaming finished:false, or empty-models guard) and no "Passing the battery" section — a user installing from PyPI and copying the PyPI quickstart scored 41/43 instead of 43/43. Repo README was compliant; only the published page was stale. FIX: publish with the repo README (battery-compliant quickstart + Passing-the-battery section). ✅ COMPLETE tick #94 (verified + board-closed; work landed in sibling commit 42668d5) — h3-harness-sdk 0.1.2 published to PyPI with compliant README (6/6 markers: history echo, do-not-finish heuristic, empty-models guard, "Passing the battery" 44/44 section, h3-test mention). Live-verified via PyPI JSON + fresh-venv install; judge PASS (verdict ca3bc6e1). | P1 | 2 | — | +hygiene, +board | deepseek-v4-flash | Publish 0.1.2 with repo README (battery-compliant quickstart + Passing-the-battery) | deepseek-v4-flash |
||||||| ✅ GAP-013 | 🟢 P2 — PyPI h3-harness-sdk 0.1.0 published wheel-only (no sdist): `pip install --no-binary h3-harness-sdk` fails "No matching distribution found" (source builds / unsupported platforms blocked). `make build` produces both wheel + sdist — only the wheel was uploaded. FIX: upload the sdist alongside the wheel in the next release. ✅ COMPLETE tick #94 (verified + board-closed; work landed in sibling commit 42668d5) — 0.1.2 published with BOTH bdist_wheel + sdist (also 0.1.1); fresh-venv `pip install --no-binary h3-harness-sdk==0.1.2` exit 0, import h3_harness → 0.1.2 (source build from sdist). judge PASS (verdict 24406f21). | P2 | 1 | — | +hygiene, +board | deepseek-v4-flash | Upload sdist alongside wheel in next release | deepseek-v4-flash |
||||||| ✅ GAP-014 | 🟢 P2 — docs/api/index.md frozen at 0.1.0: line 3 "Hand-written API reference for h3-harness-sdk 0.1.0" and line 19 `__version__ = "0.1.0"` while the published package is 0.1.2 (PyPI, 08-08). User installs 0.1.2, opens the API reference (linked from README L153), sees 0.1.0 → distrusts the doc. FIX: bump version refs to 0.1.2 (+ machine-parseable version anchor). ✅ COMPLETE tick #96 (foreman-direct, doc-only — imhotep S02 exception) — both refs bumped to 0.1.2. Pass criteria verified: grep '0.1.0' docs/api/index.md = 0 (PASS), grep '0.1.2' = 2 (PASS). 126/126 pytest, ruff 0, format 41 clean, guard PASS, commit e81c30b, co-author trailer verified. | P2 | 1 | — | +docs | deepseek-v4-flash | Bump docs/api/index.md version refs 0.1.0 → 0.1.2 | deepseek-v4-flash |
||||||| ✅ GAP-015 | 🟢 P2 — CHANGELOG.md title is "[1.0.0] — 2026-07-19" while package version is 0.1.2; zero entries for 0.1.1/0.1.2 (PyPI publish + battery-compliant README + sdist landed as 0.1.2). User checks changelog for what changed in 0.1.2 → only a 3-week-old "1.0.0" entry. FIX: rename entry to [0.1.0] and add 0.1.1/0.1.2 entries. ✅ COMPLETE tick #96 (foreman-direct, doc-only — imhotep S02 exception) — CHANGELOG.md rebuilt: [0.1.0] 2026-08-03 (initial), [0.1.1] 2026-08-08 (sdist added), [0.1.2] 2026-08-08 (battery-compliance 44/44, cancel-404, sdist hygiene). Pass criteria verified: head -5 shows '[0.1.2]' (PASS), 3 version entries ≥ 2 (PASS). 126/126 pytest, ruff 0, format 41 clean, guard PASS, commit e81c30b, co-author trailer verified. | P2 | 1 | — | +docs | deepseek-v4-flash | Rebuild CHANGELOG.md with real 0.1.x version history | deepseek-v4-flash |
